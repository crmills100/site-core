{
	"name": "site-core",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "npx wrangler pages dev",
		"deploy": "npx wrangler pages deploy",
		"db:apply": "npx wrangler d1 execute equityworkflow-forms --file=packages/db-schema/equityworkflow-d1-schema.sql",
		"db:query": "npx wrangler d1 execute equityworkflow-forms --command"
	},
	"devDependencies": {
		"wrangler": "^4.83.0"
	}
}
